induction c with d h₁ generalizing b,
rw mul_zero a,
intro h,
rw mul_eq_zero_iff at h,
cases h with a b,
exfalso,
exact ha(a),
exact b,

intro h₂,
cases b,
rw mul_zero at h₂,
symmetry at h₂,
rw mul_eq_zero_iff a (succ d) at h₂,
cases h₂,
exfalso,
exact ha(h₂),

symmetry,
exact h₂,

apply succ_eq_succ_of_eq,
rw mul_succ a b at h₂,
rw mul_succ a d at h₂,
rw add_right_cancel_iff at h₂,
have h₃ := h₁(b),
exact h₃(h₂),
