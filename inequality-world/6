cases hab with k₁ h₁,
cases hba with k₂ h₂,
rw h₂ at h₁,
symmetry at h₁,
rw add_assoc b k₂ k₁ at h₁,
have h₃ := eq_zero_of_add_right_eq_self h₁,
have h₄ := add_right_eq_zero h₃,
rw h₄ at h₂,
rw add_zero b at h₂,
exact h₂,
